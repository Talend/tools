apiVersion: v1
kind: Service
metadata:
  name: {{ include "<service_name>.service.name" . }}
  labels:
{{ include "<service_name>.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: service-port
      port: {{ .Values.service.defaultPort }}
      targetPort: service-port
  selector:
    app: {{ include "<service_name>.name" . }}
    release: {{ .Release.Name }}
