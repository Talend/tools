{{- $envValues := pluck .Values.global.env .Values | first | default .Values }}
apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: {{ template "<service_name>.fullname" . }}
  labels:
{{ include "<service_name>.labels" . | indent 4 }}
spec:
  replicas: {{ default .Values.replicaCount $envValues.replicaCount | int }}
  template:
    metadata:
      labels:
        app: {{ template "<service_name>.name" . }}
        release: {{ .Release.Name }}
    spec:
      imagePullSecrets:
        - name: {{ template "<service_name>.registryKey" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ template "<service_name>.image" . }}
          imagePullPolicy: {{ default .Values.image.pullPolicy $envValues.image.pullPolicy }}
          ports:
            - name: {{ template "tcomp-oss-all.servicePortName" . }}
              containerPort: {{ default .Values.service.defaultPort $envValues.service.defaultPort }}
          envFrom:
            - configMapRef:
                name: {{ template "<service_name>.fullname" . }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ template "tcomp-oss-all.servicePortName" . }}
            initialDelaySeconds: 5
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: {{ template "tcomp-oss-all.servicePortName" . }}
            initialDelaySeconds: 5
            timeoutSeconds: 5
          {{- if $envValues.resources }}
          resources:
{{ toYaml $envValues.resources | indent 12 }}
          {{- end }}
      {{- if $envValues.nodeAffinity }}
      affinity:
        nodeAffinity:
{{ toYaml $envValues.nodeAffinity | indent 10 }}
      {{- end }}